#
# Restic include patterns for the Plex module backup (paths relative to /srv).
# Without this file the NS8 backup would contain state/environment only.
# Syntax reference: https://pkg.go.dev/path/filepath#Glob
#
state/environment
state/plex.env
volumes/plex-config
